.row
    .col-lg-6.col-md-6.col-sm-6
        .panel.panel-primary.panel-border
            .panel-heading.border-light
                h4.panel-title
                    i.livicon(data-name='medal', data-size='16', data-loop='true', data-c='#333', data-hc='#333')
                    |                                     Tin mới trong ngày

            .panel-body

                table.table.table-hover#list-post
                    thead
                        tr
                            th(class="text-left") #
                            th(class="text-left") Tiêu đề
                            th(class="text-left") Danh mục
                    tbody
                        each post, index in listData.posts
                            tr
                                td= index + 1
                                td= post.title
                                td= post.categoryId.name

    // To do list
    .col-lg-6.col-md-6.col-sm-6
        .panel.panel-primary.todolist
            .panel-heading.border-light
                h4.panel-title
                    i.livicon(data-name='medal', data-size='18', data-color='white', data-hc='white', data-l='true')
                    |                                     Email mới
            .panel-body.nopadmar
                table.table.table-hover#list-emails
                    thead
                        tr
                            th(class="text-left") #
                            th(class="text-left") Email
                    tbody
                        each email, index in listData.emails
                            tr
                                td= index + 1
                                td= email.email
